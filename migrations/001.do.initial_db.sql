CREATE TABLE folder (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    label text NOT NULL
);

CREATE TABLE note (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  label text NOT NULL,
  content text NOT NULL,
  modtime TIMESTAMPTZ DEFAULT now() NOT NULL,
  folder_id INTEGER REFERENCES folder(id) ON DELETE CASCADE NOT NULL
);